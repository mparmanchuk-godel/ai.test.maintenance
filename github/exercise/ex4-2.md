Act as a Senior SDET designing an additional edge-case test.

Context:
- File: tests/main.navigation.refactored.spec.ts
- Navigation: Docs, API, Community

Task:
Create ONE additional edge-case test.

Requirements:
- Use Playwright (TypeScript)
- Use role/label-based selectors
- Include strong assertions
- Cover edge scenario:
  (e.g., element not visible, disabled, or incorrect navigation target)

Output:
- ONLY the test function (no imports, no full file)
- Ready to paste into existing spec

Example format:

test('...', async ({ page }) => {
  ...
});