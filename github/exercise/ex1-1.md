Act as a Senior SDET improving an existing Playwright test.

Context:
- Test already verifies visibility of navigation links: Docs, API, Community
- Stack: TypeScript + Playwright
- Page Object already exists

Task:
Enhance the test by adding navigation validation.

Requirements:
- For each navigation link:
  - Click the link
  - Verify correct page is opened (URL or unique element)
- Keep existing structure:
  // Initialization
  // User actions
  // Verification

- Use Page Object methods (do NOT use raw selectors in test)
- Add new methods in Page Object if needed (e.g., clickDocs(), clickApi(), clickCommunity())

Strict rules:
- Do NOT rewrite the whole test
- Do NOT refactor unrelated code
- Only extend functionality

Output:
1. Updated Page Object (if changed)
2. Updated test file
3. Short explanation of what was added