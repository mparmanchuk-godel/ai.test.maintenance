Act as a Senior SDET enhancing an existing GitHub Actions workflow with test analytics publishing.

Context:
- File: .github/workflows/playwright-tests.yml
- Existing job: playwright-tests

Task:
Modify ONLY the playwright-tests job to publish Playwright HTML report to GitHub Pages.

STRICT CONSTRAINTS
- Do NOT create a new job
- Do NOT remove or modify existing behavior
- Only EXTEND the existing job

Requirements:

Permissions (job-level)
Add:
permissions:
  pages: write
  id-token: write

Publishing Conditions
- Must run even if tests fail:
  if: always()
- Must run ONLY on main branch:
  github.ref == 'refs/heads/main'

Environment
- Set:
  environment: github-pages

Steps to Add (at the END of the job, in order):

1. Prepare history.json
   - Ensure playwright-report/history.json exists
   - If not, create it

2. Load previous history (if exists)
   - Checkout gh-pages branch into temp folder
   - If history.json exists there, merge it

3. Append new entry
   Include:
   - timestamp
   - github.run_url
   - github.sha
   - github.ref
   - job conclusion/status
   - test result counts if available

4. Save updated history.json back to:
   playwright-report/

5. Upload Pages artifact
   - Use: actions/upload-pages-artifact@v3
   - path: playwright-report/

6. Deploy to GitHub Pages
   - Use: actions/deploy-pages@v4

Implementation Notes
- Use bash + jq OR Node.js to safely merge JSON
- If no previous history exists:
  - create array and append current entry

Output format (STRICT)
- Return ONLY the full updated YAML file
- No explanations
- No comments outside YAML