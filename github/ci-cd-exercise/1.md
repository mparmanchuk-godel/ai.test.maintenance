Act as a Senior SDET designing a production-ready CI/CD pipeline for Playwright tests.

Task:
Create a GitHub Actions workflow at:
.github/workflows/playwright-tests.yml

Additionally:
Verify that playwright.config.ts is correctly configured for CI parallelization (workers: 3 or equivalent dynamic config for CI).  
If adjustment is needed, include a minimal diff for playwright.config.ts after the YAML.

Requirements:

Triggers
- Run ONLY on pull requests targeting the main branch:
  pull_request with branches: [ main ]

Runner & Toolchain
- Use ubuntu-latest
- Use Node.js LTS via actions/setup-node@v4
- Enable dependency caching with: cache: yarn

Install
- Install dependencies using:
  yarn install --immutable --immutable-cache --check-cache
- Install Playwright browsers with system dependencies:
  yarn playwright install --with-deps

Test Execution
- Run Playwright tests:
  yarn playwright test
- Ensure parallel execution via Playwright config (workers)
- Set environment variable:
  CI=true
- Set job timeout:
  timeout-minutes: 30

Artifacts (ALWAYS upload)
- Upload HTML report:
  path: playwright-report
  name: playwright-html-report
- Upload traces:
  path: test-results/**/trace*.zip (or default traces dir)
  name: playwright-traces
- Use:
  if: always()

Workflow Quality
- Single job named: playwright-tests
- Minimal permissions (read-only)
- Add concurrency:
  - Cancel in-progress runs for the same PR
  - Use a proper group expression

Step Order (STRICT)
1. checkout
2. setup-node
3. yarn install
4. playwright install
5. run tests
6. upload artifacts

Additional Requirements
- Include concise comments explaining key steps
- Follow clean, production-grade YAML practices

Output format (STRICT):
1. Full YAML workflow
2. (Optional, only if needed) Minimal diff for playwright.config.ts to ensure workers are correctly configured for CI parallel execution

Do NOT include any explanations outside of code blocks.